import colors from './colors';
import vars from './vars';
import sizes from './sizes';
export { globalCss, GlobalStyle } from './global.css.js';

export const theme = {
    colors,
    vars,
    sizes,
};

export default theme;
